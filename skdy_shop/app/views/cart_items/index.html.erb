<nav class="navbar navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <div class="navbar-brand">カート</div>
    </div>
  </div>
</nav>

<input type="hidden" id="flash" data-id='<%= flash[:id] %>' data-notice='<%= flash[:notice] %>' data-type='<%= flash[:type] %>' >
<div data-notice-deleted></div>
<div>
  <% @cart_items.each do |cart_item| %>
    <%= render partial: 'form', locals: {cart_item: cart_item} %>
  <% end %>
</div>

<br>
